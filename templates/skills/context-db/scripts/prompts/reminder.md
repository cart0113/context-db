context-db commands: /context-db read-manual — load context-db reading/writing
knowledge /context-db prompt "<instruction>" — consult knowledge base
/context-db pre-review "<plan>" — check standards before implementing
/context-db review "<summary>" — review changes against conventions /context-db
update "<learnings>" — file learnings /context-db maintain [path] — audit and
maintain

Flags: --mode sub-agent|main-agent --model haiku|sonnet|opus --debug
