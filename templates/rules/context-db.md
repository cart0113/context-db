This project uses `context-db/` — a hierarchical Markdown knowledge base that
documents how this project works: architecture, design decisions, gotchas, and
cross-file connections. It's a starting map to help you navigate faster and
avoid known traps. It does not contain everything — it's a starting point, a
map, a hint -- but helps you orient yourself in a project quickly.

Before starting any coding task, load the context-db skill:

```
/context-db-manual
```

If the skill is unavailable, read the manual directly:

```
Read .claude/skills/context-db-manual/SKILL.md
```

Follow its instructions for reading and updating the knowledge base.
