This project has a context-db knowledge base.

Run this on startup to get your instructions:

```
python3 .claude/skills/context-db/scripts/context-db-main-agent.py init
```

Follow the instructions it returns.
