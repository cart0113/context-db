This project has a context-db knowledge expert.

Run this to get your working instructions:

```
python3 .claude/skills/context-db-subagent/scripts/context-db-sub-agent.py instructions
```

Follow the instructions it returns.

If the script fails, load `/context-db-manual` for direct context-db access.
