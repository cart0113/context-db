<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 630">
  <!-- Background -->
  <rect width="1200" height="630" fill="#ede8e1"/>

  <!-- Icon (centered, scaled up) -->
  <g transform="translate(440, 20) scale(4.2)">
    <!-- Document body -->
    <path d="M2 2L46 2L62 18L62 78L2 78Z" fill="#d4edda" stroke="#5a9a68" stroke-width="4" stroke-linejoin="round"/>
    <!-- Folded corner -->
    <path d="M46 2L46 18L62 18" fill="#b8dcc0" stroke="#5a9a68" stroke-width="4" stroke-linejoin="round"/>
    <!-- Diagonal arrow line (bottom-left to upper-right) -->
    <path d="M20 60L40 40" fill="none" stroke="#c4a8d8" stroke-width="8" stroke-linecap="round"/>
    <!-- Arrowhead (chevron) -->
    <path d="M30 34L46 34L46 50" fill="none" stroke="#c4a8d8" stroke-width="8" stroke-linecap="round" stroke-linejoin="round"/>
  </g>

  <!-- Title -->
  <text x="600" y="470" text-anchor="middle" font-family="Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" font-weight="700" font-size="96" fill="#2d3a4a">context-db</text>

  <!-- Subtitle -->
  <text x="600" y="540" text-anchor="middle" font-family="Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif" font-weight="400" font-size="36" fill="#6b7b8d">structured knowledge for LLM agents</text>
</svg>
